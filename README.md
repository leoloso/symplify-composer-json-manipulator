# symplify-composer-json-manipulator

On 11/06/2026 the owner of `symplify/composer-json-manipulator` removed the project from GitHub, producing a 404.

Since `leoloso/monorepo-builder` has a dependency on it, on version `v10.2.2`, here we restore the project at that version.